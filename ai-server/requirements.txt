# =============================================================================
# FishDex AI Server - Dependencies
# =============================================================================

# Framework web
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Procesamiento de video/imagen
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# PyTorch (use CUDA-enabled version if already installed, or let pip install default)
torch>=2.4.1
torchvision>=0.19.1

# Fish Re-ID model dependencies
# timm: ConvNeXt backbone for FishEncoder
# ultralytics: YOLO OBB .pt detector
# torchvision.ops.DeformConv2d is included in torchvision>=0.19.1 (no extra install)
timm>=1.0.7
ultralytics>=8.3.0

# ONNX Runtime for detection/classification models
onnxruntime==1.19.2

# Scientific computing (Haversine, similarity)
scipy==1.13.1

# Appwrite Python SDK (server-side with API key)
appwrite==6.1.0

# Utilities
python-dotenv==1.0.1
pydantic==2.9.1
pydantic-settings==2.5.2
httpx==0.27.2
slowapi>=0.1.9
